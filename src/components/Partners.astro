---
// Partners Section - Institutions that trust SIMOBE
---

<section class="partners-section" id="partners">
  <div class="container">
    <div class="section-header" data-aos="fade-up">
      <h2 class="section-title">Dipercaya Oleh</h2>
      <p class="section-subtitle">
        Institusi pendidikan tinggi yang telah menggunakan SIMOBE
      </p>
    </div>

    <div class="partners-grid" data-aos="fade-up" data-aos-delay="100">
      <div class="partner-item">
        <img
          src="/img/logo-umt.png"
          alt="Universitas Muhammadiyah Tangerang"
          class="partner-logo"
          width="120"
          height="120"
          loading="lazy"
        />
        <span class="partner-name">Universitas Muhammadiyah Tangerang</span>
      </div>
    </div>
  </div>
</section>

<style>
  .partners-section {
    padding: 4rem 0;
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: 2rem;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 0.75rem;
  }

  .section-subtitle {
    color: #64748b;
    font-size: 1rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .partners-grid {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    gap: 3rem;
  }

  .partner-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 2rem;
    background: white;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .partner-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
  }

  .partner-logo {
    width: 120px;
    height: 120px;
    object-fit: contain;
  }

  .partner-name {
    font-size: 0.875rem;
    font-weight: 500;
    color: #475569;
    text-align: center;
  }

  @media (max-width: 768px) {
    .partners-section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.5rem;
    }

    .partner-logo {
      width: 100px;
      height: 100px;
    }
  }
</style>
